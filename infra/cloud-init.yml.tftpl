#cloud-config
package_update: true
runcmd:
  - |
    # Wait for workspace mount
    for i in $(seq 1 30); do
      mountpoint -q ${vm_mount_path} && break
      sleep 2
    done
    # Run setup script from mounted workspace
    bash ${vm_mount_path}/scripts/vm-setup.sh
